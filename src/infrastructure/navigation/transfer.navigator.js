import React from "react";
import { View, StyleSheet, Text } from "react-native";

const TransferNavigator = () => {
  return (
    <View>
      <Text>TransferNavigator</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TransferNavigator;
