import React from "react";
import { View, StyleSheet, Text } from "react-native";

const WalletNavigator = () => {
  return (
    <View>
      <Text>Wallet Navigator</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default WalletNavigator;
